// This file defines the main page component for the home route of the Next.js application.
// It imports and renders the Marquee component, which likely displays a scrolling marquee of content.
import Marquee from "@/components/marquee";

export default function Page() {
  // This function returns the Marquee component as the page content.
  return <Marquee />;
}